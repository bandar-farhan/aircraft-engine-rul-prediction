## Dataset

This project uses the NASA C-MAPSS FD001-FD004 datasets. The raw dataset files are not included in this repository due to size limitations. Add them to the `datasets/` folder before running the training pipeline.
